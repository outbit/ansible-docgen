{%- for host_name in data %}
## HOST: [{{ host_name }}]({{  host_name }})
{% for config_file in data[host_name] %}
 **__{{ config_file }}__**
{% for var in data[host_name][config_file]['variables'] %}
- **{{- var|upper }}**: 
{%- if data[host_name][config_file]['variables'][var] is iterable 
and not data[host_name][config_file]['variables'][var] is number 
and not data[host_name][config_file]['variables'][var] is string %}
{%- for subvar in data[host_name][config_file]['variables'][var] -%}
{%- if subvar is mapping %}
{%- for key in subvar %}                       
    - {{ key -}}: {{ subvar[key] }} 
{%- endfor %}
{%- else %}
    - {{ subvar -}}
{%- endif %}
{%- endfor -%}
{%- else %}
    {{- data[host_name][config_file]['variables'][var] -}}
{%- endif %}
{% endfor %}
{% endfor %}
{%- endfor %}

Generated by [ansible-docgen](https://www.github.com/starboarder2001/ansible-docgen)