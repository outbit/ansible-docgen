language: python
# Version to Test Against
python:
  - "3.12"
  - "3.11"
  - "3.10"

# Install Dependencies
install:
    - pip install pytest
    - pip install pyyaml
    - pip install coveralls
    - pip install jinja2
# Run Tests
script:
    - './run_tests.sh'

after_success:
    - coveralls
